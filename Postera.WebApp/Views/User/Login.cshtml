@model Postera.WebApp.Data.Models.LoginModel
@{
    ViewData["Title"] = "Login";
    Layout = "_AuthLayout";
}

<div class="login-wrap">
    <div class="login-content">
        <div class="login-logo">
            <a href="#">
                <img src="/images/logo_transparent.png" alt="Postera">
            </a>
        </div>
        <div class="login-form">
            <form asp-action="Login", asp-controller="User" method="post">
                <div class="form-group">
                    <label>Email Address</label>
                    <input class="au-input au-input--full" asp-for="Email" placeholder="Email">
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input class="au-input au-input--full" type="password" asp-for="Password" placeholder="Password">
                </div>
                <button class="au-btn au-btn--block au-btn--green m-b-20" type="submit">sign in</button>
            </form>
            <div class="register-link">
                <p>
                    Don't you have account?
                    @Html.ActionLink("Sign Up Here", "Register", "User")
                </p>
            </div>
        </div>
    </div>
</div>