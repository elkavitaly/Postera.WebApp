@using Postera.WebApp.Data.Enums
@model Postera.WebApp.Data.Models.Order

@{
    Layout = null;
    var emumValues = Enum.GetNames(typeof(OrderStatus));
    var selectListItems = emumValues.Select(x => new SelectListItem(x, x));
    var statusList = new SelectList(selectListItems, "Value", "Text", Model.Status);
}


<div class="statusChangeComponent form-group">
    <select asp-items="statusList" data-initial_state="@Model.Status" class="statusSelectList" class="form-control"></select>
    @*<div class="selectControls">
            <button id="applyStatus" class="form-control btn-primary">Apply</button>
            <button id="cancelStatus" data-initialState="" class="form-control btn-group">Cancel</button>
        </div>*@
</div>